#ifndef BITMASK_UTILS_H
#define BITMASK_UTILS_H

#include <vector>
#include <set>

int BitmaskSubsets(std::vector<int> arr, int target);

int countUniqueORs(std::vector<int> arr);

int subSetSum(std::vector<int> arr, int target);

std::vector<std::vector<int>> partitionArray(std::vector<int> arr);

#endif
